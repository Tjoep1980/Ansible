- hosts: all
  become: true
  become_user: root
  tasks:
    - name: update apt cache
      ansible.builtin.apt:
        update_cache: true
        lock_timeout: 120
      register: apt_result
      retries: 5
      delay: 15
      until: apt_result is succeeded
